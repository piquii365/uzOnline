import { Box, Stack, Typography, Button, Paper, Link } from "@mui/material";
import * as React from "react";

const ReceptionHome: React.FC = () => {
  return (
    <Box>
      <Typography>Default Reception Dashboard</Typography>
    </Box>
  );
};

export default ReceptionHome;
